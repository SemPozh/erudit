# Задача 57. Админ-API: управление пользователями, массовые уведомления

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-admin
- **Статус:** todo
- **Связанные требования:** REQ 1.14, 8.4, NFR 8

## Описание

Реализовать админ-возможности: просмотр/блокировка пользователей, отправка массовых
уведомлений, доступ к модерации контента (переиспользование задачи 20).

## Что должно получиться

- `GET /api/v1/admin/users`, `POST /api/v1/admin/users/{id}/block`.
- `POST /api/v1/admin/notifications/send-many` — массовая отправка.
- Доступ только для роли ADMIN.
- Интеграция с модерацией контента.

## Критерии готовности

- [ ] Админ управляет пользователями (блокировка).
- [ ] Массовые уведомления отправляются.
- [ ] Доступ ограничен ролью ADMIN.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 57`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/admin/users` | `—` | `200 AdminUserListResponse` | Bearer + ADMIN |
| `POST` | `/api/v1/admin/users/{id}/block` | `—` | `204 —` | Bearer + ADMIN |
| `POST` | `/api/v1/admin/notifications/send-many` | `NotificationSendManyRequest` | `200 CountResponse` | Bearer + ADMIN |

## Зависимости и проверка

Предшествующие задачи: №9, №48, №20.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
