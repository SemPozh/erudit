# Задача 51. История уведомлений, прочтение, unread

- **Исполнитель:** Семен
- **Домен (будущий микросервис):** erudit-notificator
- **Статус:** todo
- **Связанные требования:** REQ 8.13

## Описание

Предоставить пользователю историю уведомлений, отметку прочитанными и счётчик непрочитанных.

## Что должно получиться

- `GET /api/v1/notifications` — история с пагинацией.
- `POST /api/v1/notifications/{id}/read` и
  `POST /api/v1/notifications/read-all`.
- `GET /api/v1/notifications/unread-count`.

## Критерии готовности

- [ ] История уведомлений отдаётся.
- [ ] Отметка прочитанным и unread-счётчик работают.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 51`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/notifications` | `—` | `200 NotificationListResponse` | Bearer |
| `POST` | `/api/v1/notifications/{id}/read` | `—` | `204 —` | Bearer |
| `POST` | `/api/v1/notifications/read-all` | `—` | `204 —` | Bearer |
| `GET` | `/api/v1/notifications/unread-count` | `—` | `200 CountResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №48.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
