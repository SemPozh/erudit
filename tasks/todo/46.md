# Задача 46. Предпочтения нотификаций и их API

- **Исполнитель:** Семен
- **Домен (будущий микросервис):** erudit-notificator
- **Статус:** todo
- **Связанные требования:** REQ 8.9, 8.10

## Описание

Реализовать предпочтения нотификаций пользователя: какие типы получать, канал (Push/Email),
время, тихие часы. Изменения вступают в силу сразу.

## Что должно получиться

- Сущность предпочтений нотификаций (типы, каналы, время, тихие часы).
- `GET/PUT /api/v1/notifications/preferences`.
- Учёт предпочтений при планировании/отправке (доступно другим частям модуля).

## Критерии готовности

- [ ] Предпочтения читаются и обновляются.
- [ ] Изменения сразу учитываются.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 46`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/notifications/preferences` | `—` | `200 NotificationPreferencesResponse` | Bearer |
| `PUT` | `/api/v1/notifications/preferences` | `NotificationPreferences` | `200 NotificationPreferencesResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №11.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
