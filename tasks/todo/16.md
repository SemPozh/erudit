# Задача 16. Медиа: загрузка, хранение, отдача

- **Исполнитель:** Семен
- **Домен (будущий микросервис):** erudit-content
- **Статус:** todo
- **Связанные требования:** REQ 4.1

## Описание

Реализовать загрузку и хранение медиа (изображения, аудио, видео) для контента и аватаров,
а также отдачу файлов. На старте — локальное хранилище.

## Что должно получиться

- `POST /api/v1/media` — загрузка файла (admin/аватар), валидация типа и размера.
- Хранение файлов и метаданных (ключ, тип, размер).
- Отдача файлов по URL.
- Абстракция хранилища для перехода на S3 позже.

## Критерии готовности

- [ ] Файлы загружаются с валидацией типов/размера.
- [ ] Файлы отдаются по URL.
- [ ] Абстракция позволяет сменить хранилище.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 16`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/media` | `object` | `201 MediaFileResponse` | Bearer |
| `GET` | `/api/v1/media/{id}` | `—` | `200 string` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №14.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
