# Задача 17. Генерация quiz-карточек из контента

- **Исполнитель:** Семен
- **Домен (будущий микросервис):** erudit-content
- **Статус:** todo
- **Связанные требования:** REQ 4.4

## Описание

Из контента любого формата генерировать карточки-вопросы (QuizCard): формулировка факта +
вопрос + правильный ответ, используемые квизами.

## Что должно получиться

- Сущность `QuizCard` и способ генерации из контента (ручной ввод ответа или извлечение).
- `GET /api/v1/content/{id}/quiz-cards` — карточки по контенту.
- Доступ карточек домену квизов.

## Критерии готовности

- [ ] Из статьи/факта формируется корректная quiz-карточка.
- [ ] Карточки доступны домену квизов.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 17`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/content/{id}/quiz-cards` | `—` | `200 QuizCardListResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №14.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
