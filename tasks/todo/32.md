# Задача 32. Подсказки в вопросах

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-quiz
- **Статус:** todo
- **Связанные требования:** REQ 5.5

## Описание

Реализовать подсказки в вопросах квиза и учитывать их использование при начислении очков.

## Что должно получиться

- Выдача подсказки по запросу.
- Фиксация факта использования подсказки.
- Учёт подсказок при подсчёте результата/очков.

## Критерии готовности

- [ ] Подсказка выдаётся по запросу.
- [ ] Использование подсказки учитывается.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 32`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/quiz/{id}/hint` | `—` | `200 QuizCardListResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №31.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
