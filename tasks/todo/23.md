# Задача 23. Входное тестирование

- **Исполнитель:** Илья
- **Домен (будущий микросервис):** erudit-ai-platform
- **Статус:** todo
- **Связанные требования:** REQ 2.1, 2.2

## Описание

Реализовать входное тестирование при первом входе: структура теста, вопросы по тематикам,
прохождение и сохранение результата.

## Что должно получиться

- Сущности входного теста, вопросов и вариантов ответа.
- `POST /api/v1/assessment/start` и `/submit` — старт и отправка ответов.
- Сохранение результата прохождения в профиле.

## Критерии готовности

- [ ] Тест стартует и принимает ответы.
- [ ] Вопросы покрывают все тематики.
- [ ] Результат сохраняется для дальнейшей оценки.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 23`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/assessment/start` | `—` | `201 AssessmentSessionResponse` | Bearer |
| `POST` | `/api/v1/assessment/submit` | `AssessmentSubmitRequest` | `200 AssessmentResultResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №6.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
