# Задача 12. Настройки профиля

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-user
- **Статус:** todo
- **Связанные требования:** REQ 1.8, 1.9, 1.10, 3.5

## Описание

Реализовать настройки профиля: интерфейс/локализация (язык, часовой пояс), обучение
(любимые тематики, ежедневная цель), приватность (видимость в поиске/рейтинге, согласие
на аналитику). Это источник предпочтений для подбора программы и нотификаций.

## Что должно получиться

- Сущность настроек пользователя и миграция.
- API чтения/обновления настроек.
- Любимые тематики и ежедневная цель доступны другим доменам (программа, нотификации).

## Критерии готовности

- [ ] Настройки читаются и обновляются.
- [ ] Изменения сразу вступают в силу.
- [ ] Настройки согласия на аналитику учитываются.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 12`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/users/me/settings` | `—` | `200 UserSettingsResponse` | Bearer |
| `PUT` | `/api/v1/users/me/settings` | `UserSettings` | `200 UserSettingsResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №11.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
