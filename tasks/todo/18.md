# Задача 18. Каталог контента: фильтры, поиск, пагинация, сортировка

- **Исполнитель:** Семен
- **Домен (будущий микросервис):** erudit-content
- **Статус:** todo
- **Связанные требования:** REQ 4.5

## Описание

Реализовать каталог контента с фильтрами (категория, формат, сложность, премиум), поиском
по названию/описанию, пагинацией и сортировкой.

## Что должно получиться

- `GET /api/v1/content` — пагинированный список с фильтрами.
- `GET /api/v1/content/categories`, `/formats` — справочники.
- Корректное применение всех фильтров одновременно.

## Критерии готовности

- [ ] Все фильтры и поиск работают, в т.ч. вместе.
- [ ] Пагинация корректна.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 18`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/content` | `—` | `200 ContentListResponse` | публичный |
| `GET` | `/api/v1/content/categories` | `—` | `200 CategoryListResponse` | публичный |
| `GET` | `/api/v1/content/formats` | `—` | `200 FormatList` | публичный |

## Зависимости и проверка

Предшествующие задачи: №14.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
