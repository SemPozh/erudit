# Задача 38. Друзья: заявки и список

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-user
- **Статус:** todo
- **Связанные требования:** REQ 6.4, 6.5, 6.6

## Описание

Реализовать систему друзей: отправка заявки, принятие/отклонение, список друзей, рейтинг
среди друзей (данные для задачи 36).

## Что должно получиться

- Сущность связи «дружба» со статусами (заявка/друзья).
- `POST /api/v1/friends/requests` (отправка), принять/отклонить, список друзей.
- Учёт приватности.

## Критерии готовности

- [ ] Заявка отправляется и принимается/отклоняется.
- [ ] Список друзей корректен.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 38`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/friends/requests` | `FriendRequestCreate` | `201 FriendRequestListResponse` | Bearer |
| `GET` | `/api/v1/friends/requests` | `—` | `200 FriendRequestListResponse` | Bearer |
| `POST` | `/api/v1/friends/requests/{id}/accept` | `—` | `204 —` | Bearer |
| `POST` | `/api/v1/friends/requests/{id}/reject` | `—` | `204 —` | Bearer |
| `GET` | `/api/v1/friends` | `—` | `200 FriendListResponse` | Bearer |
| `DELETE` | `/api/v1/friends/{id}` | `—` | `204 —` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №37, №9.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
