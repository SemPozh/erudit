# Задача 42. Отмена подписки и окончание срока

- **Исполнитель:** Илья
- **Домен (будущий микросервис):** erudit-payment
- **Статус:** todo
- **Связанные требования:** REQ 7.7

## Описание

Реализовать отмену подписки и переход в истёкший статус по окончании оплаченного
периода. Приём вебхуков и автопродление выделены в задачу 62.

## Что должно получиться

- `POST /api/v1/subscriptions/{id}/cancel` — отмена подписки.
- Обновление статуса по сроку действия и уведомление при скором окончании.

## Критерии готовности

- [ ] Истечение срока корректно меняет статус.
- [ ] Отмена работает.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 42`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/subscriptions/{id}/cancel` | `—` | `200 SubscriptionResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №41.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
