# Задача 33. Ежедневный квиз

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-quiz
- **Статус:** todo
- **Связанные требования:** REQ 5.7

## Описание

Реализовать ежедневный квиз: генерация, одна попытка в день, сохранение результата.

## Что должно получиться

- Генерация ежедневного квиза (по прогрессу/тематикам).
- Ограничение одной попытки в день.
- Результат сохраняется и доступен.

## Критерии готовности

- [ ] Ежедневный квиз доступен раз в день.
- [ ] Повторная попытка в тот же день отклоняется.
- [ ] Результат сохраняется.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 33`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/quiz/daily` | `—` | `200 QuizAttemptResponse` | Bearer |
| `POST` | `/api/v1/quiz/daily/submit` | `QuizSubmitRequest` | `200 QuizResultResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №31.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
