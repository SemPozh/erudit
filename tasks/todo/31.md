# Задача 31. Прохождение квиза и повторение

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-quiz
- **Статус:** todo
- **Связанные требования:** REQ 5.3, 5.4, 5.6

## Описание

Реализовать прохождение квиза: старт, ответы, проверка верности, результат, начисление
очков/опыта, а также повторное прохождение.

## Что должно получиться

- `POST /api/v1/quiz/{id}/start` и `/submit` — прохождение и результат.
- Проверка ответов и начисление очков/опыта.
- Хранение попыток и повторное прохождение квиза.

## Критерии готовности

- [ ] Ответы проверяются, очки начисляются.
- [ ] Повторное прохождение сохраняет новую попытку.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 31`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/quiz/{id}/start` | `—` | `201 QuizAttemptResponse` | Bearer |
| `POST` | `/api/v1/quiz/{id}/submit` | `QuizSubmitRequest` | `200 QuizResultResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №30, №9.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
