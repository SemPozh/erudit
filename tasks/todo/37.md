# Задача 37. Поиск пользователей

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-user
- **Статус:** todo
- **Связанные требования:** REQ 6.3

## Описание

Реализовать поиск других пользователей по имени/email с учётом настроек приватности.

## Что должно получиться

- `GET /api/v1/users/search?query=` — поиск по имени/email.
- Пагинация и фильтрация по приватности (скрытые профили исключаются).

## Критерии готовности

- [ ] Поиск по имени и email работает.
- [ ] Пользователи со скрытым профилем не попадают в поиск.
- [ ] Покрыто тестами.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 37`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v1/users/search` | `—` | `200 AdminUserListResponse` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №11, №12.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
