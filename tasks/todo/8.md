# Задача 8. Авторизация JWT (access/refresh), вход, выход

- **Исполнитель:** Дестини
- **Домен (будущий микросервис):** erudit-user
- **Статус:** todo
- **Связанные требования:** REQ 1.2, 1.3, 1.12

## Описание

Реализовать вход по email/паролю и механику токенов доступа и обновления (access/refresh),
включая выход.

## Что должно получиться

- `POST /api/v1/auth/login` — выдаёт access и refresh токены.
- `POST /api/v1/auth/refresh` — обновление access по refresh.
- `POST /api/v1/auth/logout` — инвалидация/отзыв refresh-токена.
- Хранение/валидация refresh-токенов.

## Критерии готовности

- [ ] Вход выдаёт токены; неверные креды отклоняются (401).
- [ ] Refresh обновляет access; отозванный refresh не принимается.
- [ ] Выход отзывает refresh-токен.

## Привязка к OpenAPI

Источник истины: `src/main/resources/static/openapi.yaml` (`x-task-id: 8`). Поля и коды ошибок берутся из соответствующих схем контракта.

| Метод | Путь | Тело запроса | Успешный ответ | Доступ |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/auth/login` | `LoginRequest` | `200 TokenPairResponse` | публичный |
| `POST` | `/api/v1/auth/refresh` | `RefreshRequest` | `200 TokenPairResponse` | публичный |
| `POST` | `/api/v1/auth/logout` | `RefreshRequest` | `204 —` | Bearer |

## Зависимости и проверка

Предшествующие задачи: №6.

Проверить положительный сценарий, ошибки валидации и доступа, а также сохранение/чтение данных там, где это применимо. Для HTTP-маршрутов сверить фактические тела и статусы с OpenAPI и выполнить `./gradlew test openApiValidate build`.
