spring:
  application:
    name: erudit
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/erudit}
    username: ${DB_USERNAME:erudit}
    password: ${DB_PASSWORD:erudit}
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8080}
